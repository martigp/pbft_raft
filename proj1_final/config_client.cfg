/**
  * The address for the RaftClient
  */
listenAddress = "127.0.0.1";

/**
  * The port used for RaftClient.
  */
raftPort = 1234;

/**
  * A list of server information. Specifically their unique ID and
  * the IP address they are listening for incoming connections on.
  */

servers = ( { id = 1;
             address = "127.0.0.1"; 
             port = 1234; },
             { id = 2;
             address = "127.0.0.1"; 
             port = 2222; },
            { id = 3;
             address = "127.0.0.1"; 
             port = 4421; } );



